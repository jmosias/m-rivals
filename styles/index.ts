import {StyleSheet} from 'react-native';

export const viewStyles = StyleSheet.create({
  centered: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  column: {
    flex: 1,
  },
});
